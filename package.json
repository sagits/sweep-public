{
  "name": "sweep-hosts",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@12.3.4",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "build:web": "turbo run build:web",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "e2e:build": "turbo run e2e:build",
    "e2e:test": "turbo run e2e:test"
  },
  "devDependencies": {
    "turbo": "^2.10.12",
    "typescript": "~5.9.3"
  }
}
