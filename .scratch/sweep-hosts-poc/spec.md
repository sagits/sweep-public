# Sweep Hosts PoC — spec

The spec is `poc/PRD.md` at the repo root, with the reference screenshots in `poc/screenshots/`.

The screenshots are the source of truth for layout. Where the PRD text and a screenshot
disagree, follow the screenshot and flag the conflict in `DECISIONS.md`.

Tickets live in `issues/`, numbered in dependency order.
