import type { User } from '@sweep/types';

/** The app boots straight into the logged-in state for this user. */
export const currentUser: User = {
  name: 'Renato Probst',
  email: 'renatopprobst@gmail.com',
};
